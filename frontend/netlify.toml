[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Enable demo mode for Netlify deployment (no backend required)
  VITE_DEMO_MODE = "true"

# Handle SPA routing - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
