# API URL (optional, defaults to http://localhost:8000)
VITE_API_URL=http://localhost:8000

# Demo mode - set to 'true' to simulate API responses without backend
# Useful for demos, testing UI, or deploying frontend-only
VITE_DEMO_MODE=false
